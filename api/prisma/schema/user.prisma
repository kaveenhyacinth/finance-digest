model User {
  id        Int      @id @default(sequence())
  firstName String
  lastName  String
  email     String   @unique
  password  String
  posts     Post[]
  createAt  DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
}
