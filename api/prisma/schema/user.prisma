model User {
  id        Int      @id @default(sequence())
  email     String   @unique
  password  String
  posts     Post[]
  createAt  DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
}
